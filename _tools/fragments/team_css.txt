
/* ============================================================
   OUR TEAM
   ============================================================ */
.hero--book.hero--team h1{white-space:normal;max-width:14ch;}
.hero--team .hero__sub{max-width:96ch;}
.btn--blue{background:var(--blue);color:#fff;}
.btn--blue::before{background:var(--navy);}
.btn--blue:hover::before{transform:scaleX(1);}

/* ---- intro ---- */
.tm-intro{display:grid;grid-template-columns:1.02fr .98fr;gap:clamp(30px,5vw,74px);align-items:start;}
.tm-intro h2{font-size:clamp(28px,3.9vw,52px);font-weight:700;line-height:1.06;}
.tm-intro h2 em{font-style:normal;color:var(--blue);}
.tm-intro__body p{color:var(--grey);font-size:16px;line-height:1.75;margin-bottom:16px;}
.tm-intro__body p:last-child{margin-bottom:0;}
.tm-intro__body .lead{color:var(--ink);font-size:clamp(17px,1.5vw,19.5px);font-weight:500;line-height:1.6;}
.tm-pm{margin-top:clamp(26px,3vw,34px);display:flex;align-items:center;gap:16px;background:var(--blue-soft);border-radius:16px;padding:18px 22px;}
.tm-pm i{flex:0 0 auto;width:42px;height:42px;border-radius:12px;background:#fff;display:grid;place-items:center;}
.tm-pm i svg{width:20px;height:20px;fill:none;stroke:var(--blue);stroke-width:1.9;stroke-linecap:round;stroke-linejoin:round;}
.tm-pm span{font-size:14.6px;color:var(--navy);line-height:1.6;font-weight:500;}

/* ---- the grid: 3 columns, full-width expanding panels ---- */
.tm-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));column-gap:clamp(14px,1.8vw,22px);row-gap:0;}
.tm-card{position:relative;display:block;width:100%;text-align:left;font-family:inherit;background:#fff;border:1px solid var(--line);border-radius:calc(var(--radius) + 4px);padding:0;overflow:hidden;cursor:pointer;
  margin-bottom:clamp(14px,1.8vw,22px);transition:transform .42s var(--ease),box-shadow .42s var(--ease),border-color .35s,background .45s;}
.tm-card:hover{transform:translateY(-7px);box-shadow:0 46px 90px -46px rgba(12,27,42,.5);border-color:rgba(20,135,216,.4);}
.tm-card:focus-visible{outline:2px solid var(--blue);outline-offset:3px;}
.tm-card.is-open{background:linear-gradient(160deg,var(--navy),var(--navy-deep));border-color:transparent;transform:translateY(-7px);box-shadow:0 46px 90px -46px rgba(12,27,42,.6);}

/* photo tile */
.tm-photo{display:block;position:relative;aspect-ratio:1/1;overflow:hidden;background:linear-gradient(150deg,var(--navy),var(--navy-deep));}
.tm-photo img{width:100%;height:100%;object-fit:cover;object-position:center 22%;filter:grayscale(1) contrast(1.04) brightness(1.02);transform:scale(1.02);
  transition:filter .6s var(--ease),transform .9s var(--ease);}
.tm-card:hover .tm-photo img,.tm-card.is-open .tm-photo img{filter:grayscale(0) contrast(1) brightness(1);transform:scale(1.08);}
.tm-photo::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(14,36,64,0) 34%,rgba(14,36,64,.62) 100%);opacity:.9;transition:opacity .45s;}
.tm-card:hover .tm-photo::after{opacity:.6;}
/* monogram fallback when a portrait file isn't there yet */
.tm-mono{position:absolute;inset:0;display:none;place-items:center;font-family:var(--font-display);font-weight:800;font-size:clamp(46px,5.4vw,72px);letter-spacing:.02em;color:rgba(255,255,255,.94);}
.tm-photo.is-mono img{display:none;}
.tm-photo.is-mono .tm-mono{display:grid;}
.tm-photo.is-mono{background:linear-gradient(150deg,var(--navy) 0%,#1d4d7f 48%,var(--blue) 100%);background-size:180% 180%;animation:tmSheen 9s ease-in-out infinite;}
.tm-grid > .tm-card:nth-of-type(3n+2) .tm-photo.is-mono{background:linear-gradient(150deg,#0e2440 0%,#17527f 50%,#3aa0e0 100%);background-size:180% 180%;}
.tm-grid > .tm-card:nth-of-type(3n) .tm-photo.is-mono{background:linear-gradient(150deg,#12395f 0%,var(--blue) 55%,#7cc3ef 100%);background-size:180% 180%;}
@keyframes tmSheen{0%,100%{background-position:0% 50%;}50%{background-position:100% 50%;}}
.tm-idx{display:block;position:absolute;left:18px;top:14px;z-index:2;font-family:var(--font-display);font-size:12px;font-weight:700;letter-spacing:.2em;color:rgba(255,255,255,.72);}
.tm-flag{position:absolute;right:16px;bottom:14px;z-index:2;font-size:10.5px;font-weight:600;letter-spacing:.14em;text-transform:uppercase;color:#fff;background:rgba(255,255,255,.18);backdrop-filter:blur(6px);padding:5px 11px;border-radius:100px;}

/* card body */
.tm-body{display:block;position:relative;padding:clamp(20px,2.2vw,26px) clamp(20px,2.2vw,26px) clamp(22px,2.4vw,28px);}
.tm-name{display:block;font-family:var(--font-display);font-size:clamp(18px,1.8vw,22px);font-weight:700;color:var(--ink);line-height:1.2;transition:color .35s;}
.tm-role{display:block;margin-top:7px;font-size:12px;font-weight:600;letter-spacing:.14em;text-transform:uppercase;color:var(--blue);transition:color .35s;}
.tm-rule{display:block;height:2px;width:26px;margin-top:16px;border-radius:2px;background:var(--blue);transform-origin:left;transition:transform .5s var(--ease),background .35s;}
.tm-card:hover .tm-rule{transform:scaleX(2.6);}
.tm-more{margin-top:16px;display:flex;align-items:center;justify-content:space-between;gap:12px;font-size:13.5px;font-weight:600;color:var(--grey);transition:color .35s;}
.tm-plus{flex:0 0 auto;position:relative;width:30px;height:30px;border-radius:50%;background:var(--blue-soft);transition:background .35s,transform .5s var(--ease);}
.tm-plus::before,.tm-plus::after{content:"";position:absolute;left:50%;top:50%;width:12px;height:2px;border-radius:2px;background:var(--blue);transform:translate(-50%,-50%);transition:transform .45s var(--ease),background .35s;}
.tm-plus::after{transform:translate(-50%,-50%) rotate(90deg);}
.tm-card:hover .tm-plus{transform:rotate(90deg);}
.tm-card.is-open .tm-name{color:#fff;}
.tm-card.is-open .tm-role{color:#7cc3ef;}
.tm-card.is-open .tm-more{color:rgba(255,255,255,.7);}
.tm-card.is-open .tm-rule{background:#7cc3ef;transform:scaleX(2.6);}
.tm-card.is-open .tm-plus{background:rgba(124,195,239,.2);transform:rotate(90deg);}
.tm-card.is-open .tm-plus::before,.tm-card.is-open .tm-plus::after{background:#7cc3ef;}
.tm-card.is-open .tm-plus::after{transform:translate(-50%,-50%) rotate(0deg);}

/* expanding panel */
.tm-panel{display:none;}
.tm-panel.is-mounted{display:grid;grid-column:1/-1;grid-template-rows:0fr;margin-bottom:0;
  transition:grid-template-rows .6s var(--ease),margin-bottom .5s var(--ease);}
.tm-panel.is-open{grid-template-rows:1fr;margin-bottom:clamp(14px,1.8vw,22px);}
.tm-panel__clip{overflow:hidden;min-height:0;}
.tm-panel__box{position:relative;overflow:hidden;background:linear-gradient(155deg,var(--navy) 0%,var(--navy-deep) 100%);color:#fff;border-radius:calc(var(--radius) + 6px);
  padding:clamp(26px,3.4vw,48px);display:grid;grid-template-columns:minmax(0,.34fr) minmax(0,.66fr);gap:clamp(26px,3.6vw,54px);
  opacity:0;transform:translateY(16px);transition:opacity .5s .1s var(--ease),transform .6s .1s var(--ease);}
.tm-panel.is-open .tm-panel__box{opacity:1;transform:none;}
.tm-panel__box::before{content:"";position:absolute;right:-90px;top:-90px;width:340px;height:340px;border-radius:50%;background:radial-gradient(circle,rgba(20,135,216,.4),transparent 66%);pointer-events:none;}
.tm-panel__aside{position:relative;z-index:1;}
.tm-shot{position:relative;aspect-ratio:1/1;border-radius:var(--radius);overflow:hidden;background:linear-gradient(150deg,#12395f,var(--blue));}
.tm-shot img{width:100%;height:100%;object-fit:cover;object-position:center 22%;}
.tm-shot.is-mono img{display:none;}
.tm-shot .tm-mono{color:#fff;font-size:clamp(52px,6vw,84px);}
.tm-shot.is-mono .tm-mono{display:grid;}
.tm-motto{margin-top:22px;border-left:3px solid #7cc3ef;padding-left:18px;}
.tm-motto b{display:block;font-size:11px;font-weight:600;letter-spacing:.18em;text-transform:uppercase;color:#7cc3ef;margin-bottom:9px;}
.tm-motto q{font-family:var(--font-display);font-size:clamp(15.5px,1.4vw,18px);font-weight:600;line-height:1.45;color:#fff;}
.tm-panel__main{position:relative;z-index:1;}
.tm-panel__head{display:flex;flex-wrap:wrap;align-items:baseline;gap:10px 18px;padding-bottom:20px;border-bottom:1px solid rgba(255,255,255,.16);}
.tm-panel__head h3{color:#fff;font-size:clamp(24px,2.9vw,38px);font-weight:700;line-height:1.08;}
.tm-panel__head span{font-size:12px;font-weight:600;letter-spacing:.15em;text-transform:uppercase;color:#7cc3ef;}
.tm-bio{margin-top:22px;column-count:2;column-gap:clamp(24px,2.6vw,40px);}
.tm-bio p{color:rgba(255,255,255,.8);font-size:15.2px;line-height:1.78;margin-bottom:15px;break-inside:avoid;}
.tm-bio p:last-child{margin-bottom:0;}
.tm-close{margin-top:26px;display:inline-flex;align-items:center;gap:10px;font-family:var(--font-display);font-size:14px;font-weight:600;color:#fff;background:rgba(255,255,255,.1);border:0;border-radius:100px;padding:11px 20px;cursor:pointer;transition:background .3s,gap .3s var(--ease);}
.tm-close:hover{background:rgba(124,195,239,.24);gap:14px;}
.tm-close svg{width:15px;height:15px;fill:none;stroke:currentColor;stroke-width:2.2;stroke-linecap:round;}

/* countries band */
.tm-reach{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:clamp(14px,1.8vw,24px);}
.tm-loc{position:relative;background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.13);border-radius:var(--radius);padding:clamp(24px,2.8vw,36px);transition:background .35s,transform .4s var(--ease),border-color .35s;}
.tm-loc:hover{background:rgba(255,255,255,.09);transform:translateY(-6px);border-color:rgba(124,195,239,.4);}
.tm-loc b{display:block;font-family:var(--font-display);font-size:clamp(20px,2.1vw,27px);font-weight:700;color:#fff;}
.tm-loc span{display:block;margin-top:11px;font-size:14.6px;color:rgba(255,255,255,.7);line-height:1.7;}
.tm-loc i{position:absolute;right:22px;top:22px;font-style:normal;font-family:var(--font-display);font-size:12px;font-weight:700;letter-spacing:.16em;color:rgba(124,195,239,.75);}

@media (max-width:1080px){
  .tm-grid{grid-template-columns:repeat(2,minmax(0,1fr));}
  .tm-bio{column-count:1;}
}
@media (max-width:900px){
  .tm-intro{grid-template-columns:1fr;}
  .tm-panel__box{grid-template-columns:1fr;}
  .tm-panel__aside{max-width:320px;}
  .tm-reach{grid-template-columns:1fr;}
}
@media (max-width:620px){
  .tm-grid{grid-template-columns:1fr;}
}
@media (prefers-reduced-motion:reduce){
  .tm-photo.is-mono{animation:none;}
  .tm-card,.tm-panel,.tm-panel__box,.tm-photo img{transition-duration:.01ms;}
}

/* no-JS fallback: every profile stays readable */
.no-js .tm-panel{display:grid!important;grid-column:1/-1;grid-template-rows:1fr!important;margin-bottom:clamp(14px,1.8vw,22px);}
.no-js .tm-panel__box{opacity:1!important;transform:none!important;}
.no-js .tm-plus,.no-js .tm-close{display:none!important;}
