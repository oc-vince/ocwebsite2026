
/* ============================================================
   BLOG POST — page-specific styles
   ============================================================ */
.hero--book{padding:clamp(130px,16vh,180px) 0 clamp(78px,14vh,160px);}
.hero--book h1{line-height:1.1;}
.hero--blog h1{white-space:normal;max-width:88ch;font-size:clamp(21px,7vw,46px);letter-spacing:-.025em;
  text-wrap:balance;}
.hero--blog .hero__eyebrow{margin-bottom:20px;}
.hero--blog .hero__eyebrow::before{width:26px;}

.bp{position:relative;padding:clamp(58px,7.5vh,96px) 0 clamp(80px,10vh,130px);background:var(--white);}
.bp::before{content:"";position:absolute;left:0;right:0;top:0;height:280px;pointer-events:none;z-index:0;
  background:linear-gradient(180deg,rgba(20,135,216,.05),rgba(255,255,255,0));}
.bp > .container{position:relative;z-index:1;max-width:var(--max);}
.bp__article{position:relative;}
.bp__article::after{content:"";display:block;clear:both;}

/* ---------- featured image: floats right at 40%, copy wraps around ---------- */
.bp-figure{position:relative;float:right;width:40%;max-width:520px;margin:4px 0 36px 52px;}
.bp-figure::before{content:"";position:absolute;inset:20px -20px -20px 20px;border-radius:24px;z-index:0;
  background:linear-gradient(155deg,var(--blue-soft),rgba(20,135,216,.05));}
.bp-figure__shot{position:relative;z-index:1;overflow:hidden;border-radius:20px;background:var(--mist);
  box-shadow:0 28px 64px -30px rgba(12,27,42,.55);}
.bp-figure__shot::after{content:"";position:absolute;inset:0;pointer-events:none;border-radius:20px;
  box-shadow:inset 0 0 0 1px rgba(255,255,255,.35);}
.bp-figure__shot img{width:100%;height:auto;display:block;transition:transform 1.1s var(--ease);}
.bp-figure:hover .bp-figure__shot img{transform:scale(1.05);}
.bp-figure__cap{position:relative;z-index:1;display:none;margin-top:20px;padding:24px 26px 26px;border-radius:18px;
  background:var(--navy-deep);color:#fff;overflow:hidden;}
.bp-figure__cap::before{content:"";position:absolute;inset:0;pointer-events:none;
  background:radial-gradient(70% 90% at 92% 4%,rgba(20,135,216,.42),transparent 64%);}
.bp-figure__cap.is-on{display:block;}
.bp-figure__kicker{position:relative;z-index:2;display:inline-flex;align-items:center;gap:9px;
  font-size:11.5px;font-weight:600;letter-spacing:.2em;text-transform:uppercase;color:#7cc3ef;margin-bottom:14px;}
.bp-figure__kicker::before{content:"";width:22px;height:1.5px;background:#7cc3ef;}
.bp-figure__toc{position:relative;z-index:2;counter-reset:bptoc;list-style:none;margin:0;padding:0;}
.bp-figure__toc li{counter-increment:bptoc;position:relative;padding:9px 0 9px 34px;font-size:14.5px;line-height:1.45;
  color:rgba(255,255,255,.86);border-top:1px solid rgba(255,255,255,.12);}
.bp-figure__toc li:first-child{border-top:0;padding-top:2px;}
.bp-figure__toc li::before{content:counter(bptoc,decimal-leading-zero);position:absolute;left:0;top:9px;
  font-family:var(--font-display);font-size:12px;font-weight:700;color:var(--blue);letter-spacing:.06em;}
.bp-figure__toc li:first-child::before{top:2px;}

/* ---------- article typography (styling only — markup untouched) ---------- */
.post-content{counter-reset:bpstep;font-size:17px;line-height:1.85;color:#3c4c5c;}
.post-content p{margin:0 0 26px;}
.post-content a{color:var(--blue);font-weight:600;box-shadow:inset 0 -1px 0 rgba(20,135,216,.38);
  transition:color .3s var(--ease),box-shadow .3s var(--ease);}
.post-content a:hover{color:var(--navy);box-shadow:inset 0 -2px 0 var(--blue);}
.post-content h2{font-size:clamp(24px,2.6vw,34px);font-weight:700;margin:44px 0 16px;}
.post-content h3{font-size:clamp(20px,2.1vw,26px);font-weight:700;margin:38px 0 14px;}
.post-content h4{font-size:18px;font-weight:600;margin:32px 0 12px;}
.post-content img{border-radius:16px;margin:8px 0 28px;}
.post-content blockquote{margin:34px 0;padding:6px 0 6px 28px;border-left:3px solid var(--blue);
  font-family:var(--font-display);font-size:20px;line-height:1.5;color:var(--navy);}
.post-content table{width:100%;border-collapse:collapse;margin:0 0 28px;font-size:15.5px;}
.post-content table th,.post-content table td{border:1px solid var(--line);padding:12px 14px;text-align:left;}
.post-content table th{background:var(--paper);font-weight:600;color:var(--navy);}
.post-content iframe{max-width:100%;border-radius:16px;}
.post-content figure{margin:8px 0 28px;}
.post-content figure img{margin:0;}
.post-content figcaption{margin-top:10px;font-size:13.5px;color:var(--grey-light);}
.post-content em{font-style:italic;}
.post-content > center{display:block;margin:0 0 26px;}
.post-content ol{margin:0 0 26px;padding-left:22px;list-style:decimal;}
.post-content ul{margin:0 0 26px;padding-left:22px;list-style:disc;}
.post-content li{margin:0 0 8px;}
.post-content li::marker{color:var(--blue);}

/* WordPress posts use <ul><li><strong>Heading</strong></li></ul> as a sub-heading.
   :has() targets exactly that shape and leaves genuine bullet lists alone. */
.post-content ul:has(> li:only-child > strong:only-child){margin:46px 0 20px;padding:0;list-style:none;}
.post-content ul:has(> li:only-child > strong:only-child) > li{position:relative;margin:0;padding-left:66px;list-style:none;}
.post-content ul:has(> li:only-child > strong:only-child) > li::marker{content:none;}
.post-content ul:has(> li:only-child > strong:only-child) > li > strong{display:block;font-family:var(--font-display);
  font-weight:700;font-size:clamp(20px,2.2vw,27px);line-height:1.14;letter-spacing:-.02em;color:var(--ink);}
.post-content ul:has(> li:only-child > strong:only-child) > li::before{counter-increment:bpstep;
  content:counter(bpstep,decimal-leading-zero);position:absolute;left:0;top:3px;font-family:var(--font-display);
  font-size:13.5px;font-weight:700;letter-spacing:.06em;color:var(--blue);}
.post-content ul:has(> li:only-child > strong:only-child) > li::after{content:"";position:absolute;left:0;top:26px;
  width:46px;height:2px;background:linear-gradient(90deg,var(--blue),rgba(20,135,216,0));}

@media (max-width:1100px){
  .bp-figure{width:44%;margin-left:40px;}
}
@media (max-width:900px){
  .bp-figure{float:none;width:100%;max-width:none;margin:0 0 38px;display:flex;flex-direction:column;}
  .bp-figure::before{display:none;}
  .bp-figure__cap{order:1;margin-top:0;margin-bottom:22px;}
  .bp-figure__shot{order:2;}
  .post-content{font-size:16.5px;}
  .post-content ul:has(> li:only-child > strong:only-child) > li{padding-left:0;padding-top:30px;}
  .post-content ul:has(> li:only-child > strong:only-child) > li::after{top:20px;}
}

/* crumbs follow the hero's centred layout on tablet & mobile */
@media (max-width:900px){
  .hero--blog .crumbs{justify-content:center;text-align:center;}
}
@media (max-width:480px){
  .crumbs{margin-bottom:48px;}
}
