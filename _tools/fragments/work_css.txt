
/* ============================================================
   OUR WORK
   ============================================================ */
.hero--book.hero--work h1{white-space:normal;max-width:12ch;}
.hero--work .hero__sub{max-width:100ch;}
.btn--blue{background:var(--blue);color:#fff;}
.btn--blue::before{background:var(--navy);}
.btn--blue:hover::before{transform:scaleX(1);}

/* ---- case study hero image ---- */
.cs__shot{display:block;position:relative;z-index:1;overflow:hidden;border-radius:calc(var(--radius) + 4px);
  background:var(--mist);box-shadow:0 40px 84px -44px rgba(12,27,42,.52);transition:transform .55s var(--ease),box-shadow .55s var(--ease);}
.cs__shot img{display:block;width:100%;height:auto;transition:transform 1.2s var(--ease);}
.cs__shot:hover img{transform:scale(1.05);}
.cs__shot::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(12,27,42,0) 62%,rgba(12,27,42,.34) 100%);opacity:0;transition:opacity .45s;}
.cs__shot:hover::after{opacity:1;}
.cs__go{position:absolute;right:18px;bottom:18px;z-index:2;width:46px;height:46px;border-radius:50%;background:rgba(255,255,255,.95);display:grid;place-items:center;
  opacity:0;transform:translateY(10px) scale(.9);transition:opacity .4s var(--ease),transform .5s var(--ease),background .3s;box-shadow:0 14px 30px rgba(12,27,42,.34);}
.cs__go svg{width:19px;height:19px;fill:none;stroke:var(--navy);stroke-width:2.1;stroke-linecap:round;stroke-linejoin:round;transition:stroke .3s;}
.cs__shot:hover .cs__go{opacity:1;transform:none;}
.cs__go:hover,.cs__shot:hover .cs__go:hover{background:var(--blue);}
.cs__shot:hover .cs__go:hover svg{stroke:#fff;}

/* ---- case study carousel (full-bleed) ---- */
.csx{position:relative;width:100%;overflow:hidden;}
.csx__viewport{overflow:hidden;cursor:grab;transition:height .7s var(--ease);}
.csx__viewport.is-dragging{cursor:grabbing;}
.csx__track{display:flex;align-items:flex-start;will-change:transform;transition:transform .78s var(--ease);}
.csx__track.no-anim{transition:none;}
.csx__slide{flex:0 0 100%;min-width:0;}
.csx__inner{max-width:var(--max);margin:0 auto;padding:0 var(--gutter);
  display:grid;grid-template-columns:minmax(0,1.04fr) minmax(0,.96fr);gap:clamp(28px,4.4vw,68px);align-items:center;}
.csx__slide:nth-child(even) .cs__media{order:2;}
/* content settles in only for the slide on screen */
.csx__slide .cs__body > *,.csx__slide .cs__media{opacity:0;transform:translateY(20px);transition:opacity .6s var(--ease),transform .7s var(--ease);}
.csx__slide.is-current .cs__body > *,.csx__slide.is-current .cs__media{opacity:1;transform:none;}
.csx__slide.is-current .cs__body > *:nth-child(1){transition-delay:.10s;}
.csx__slide.is-current .cs__body > *:nth-child(2){transition-delay:.16s;}
.csx__slide.is-current .cs__body > *:nth-child(3){transition-delay:.22s;}
.csx__slide.is-current .cs__body > *:nth-child(4){transition-delay:.28s;}
.csx__slide.is-current .cs__body > *:nth-child(5){transition-delay:.34s;}
.csx__slide.is-current .cs__media{transition-delay:.06s;}

.cs__media{position:relative;}
.csx__slide:hover .cs__shot{transform:translateY(-8px);box-shadow:0 56px 104px -48px rgba(12,27,42,.6);}
.cs__idx{position:absolute;left:-18px;top:-30px;z-index:0;font-family:var(--font-display);font-size:clamp(56px,7vw,104px);font-weight:800;line-height:1;
  color:var(--white);-webkit-text-stroke:1.5px var(--line);pointer-events:none;}
.cs__tag{display:inline-flex;align-items:center;gap:9px;font-size:11.5px;font-weight:600;letter-spacing:.16em;text-transform:uppercase;color:var(--blue);background:var(--blue-soft);padding:7px 15px;border-radius:100px;}
.cs__body h3{font-size:clamp(25px,3.2vw,44px);font-weight:700;line-height:1.06;margin-top:18px;letter-spacing:-.02em;}
.cs__blurb{margin-top:16px;color:var(--grey);font-size:16px;line-height:1.75;max-width:56ch;}
.cs__handle{margin-top:clamp(22px,2.6vw,30px);}
.cs__handle b{display:block;font-size:11.5px;font-weight:600;letter-spacing:.16em;text-transform:uppercase;color:var(--grey-light);margin-bottom:14px;}
.cs__handle ul{display:grid;gap:2px;}
.cs__handle li{display:grid;grid-template-columns:21px 1fr;gap:12px;align-items:start;padding:11px 0;border-bottom:1px solid var(--line);font-size:15px;line-height:1.6;color:var(--ink);}
.cs__handle li:last-child{border-bottom:0;}
.cs__handle li svg{display:block;width:21px;height:21px;fill:none;stroke:var(--blue);stroke-width:2.1;stroke-linecap:round;stroke-linejoin:round;margin-top:1px;}
.cs__visit{display:inline-flex;align-items:center;gap:10px;margin-top:clamp(22px,2.6vw,30px);font-family:var(--font-display);font-size:15px;font-weight:600;color:var(--navy);transition:color .3s,gap .3s var(--ease);}
.cs__visit svg{width:17px;height:17px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;}
.cs__visit:hover{color:var(--blue);gap:15px;}

/* ---- carousel controls ---- */
.csx-bar{max-width:var(--max);margin:clamp(30px,4vw,50px) auto 0;padding:0 var(--gutter);
  display:flex;flex-wrap:wrap;align-items:center;gap:clamp(16px,2.4vw,32px);}
.csx-arrows{display:flex;gap:10px;}
.csx-arrow{width:52px;height:52px;border:1.5px solid var(--line);border-radius:50%;background:#fff;color:var(--navy);cursor:pointer;display:grid;place-items:center;
  transition:background .3s,border-color .3s,color .3s,transform .35s var(--ease);}
.csx-arrow:hover{background:var(--navy);border-color:var(--navy);color:#fff;transform:translateY(-3px);}
.csx-arrow:disabled{opacity:.32;cursor:default;transform:none;background:#fff;border-color:var(--line);color:var(--navy);}
.csx-arrow svg{width:20px;height:20px;fill:none;stroke:currentColor;stroke-width:2.1;stroke-linecap:round;stroke-linejoin:round;}
.csx-dots{display:flex;align-items:center;gap:9px;}
.csx-dot{width:9px;height:9px;padding:0;border:0;border-radius:50%;background:var(--line);cursor:pointer;transition:width .4s var(--ease),background .3s;}
.csx-dot:hover{background:var(--grey-light);}
.csx-dot.is-active{width:32px;border-radius:100px;background:var(--blue);}
.csx-count{margin-left:auto;display:flex;align-items:baseline;gap:5px;font-family:var(--font-display);font-size:14px;font-weight:600;color:var(--grey-light);}
.csx-count b{font-size:22px;color:var(--navy);}
.csx-rail{flex:1 1 180px;min-width:120px;height:3px;border-radius:3px;background:var(--mist);overflow:hidden;}
.csx-rail i{display:block;height:100%;border-radius:3px;background:linear-gradient(90deg,var(--navy),var(--blue));transform-origin:left;transition:transform .78s var(--ease);}

@media (max-width:1000px){
  .pf-grid{grid-template-columns:repeat(2,minmax(0,1fr));}
}
@media (max-width:900px){
  .csx__inner{grid-template-columns:1fr;}
  .csx__slide:nth-child(even) .cs__media{order:0;}
  .cs__idx{left:-6px;top:-16px;}
  .csx-count{margin-left:0;}
  .csx-rail{flex-basis:100%;order:5;}
}
@media (prefers-reduced-motion:reduce){
  .csx__track{transition:none;}
  .csx__slide .cs__body > *,.csx__slide .cs__media{opacity:1;transform:none;transition:none;}
}

/* ---- portfolio grid + filter ---- */
.pf-filters{display:flex;flex-wrap:wrap;gap:10px;margin-bottom:clamp(26px,3.2vw,38px);}
.pf-filter{position:relative;font-family:var(--font-display);font-size:13.5px;font-weight:600;color:var(--grey);background:#fff;border:1px solid var(--line);border-radius:100px;padding:12px 24px;cursor:pointer;
  transition:color .35s var(--ease),background .45s var(--ease),border-color .35s var(--ease),transform .35s var(--ease),box-shadow .4s var(--ease);}
.pf-filter:hover{border-color:var(--blue);color:var(--blue);transform:translateY(-3px);box-shadow:0 14px 28px -14px rgba(20,135,216,.5);}
.pf-filter.is-active{background:var(--navy);border-color:var(--navy);color:#fff;transform:none;box-shadow:0 16px 32px -16px rgba(12,27,42,.55);}
.pf-filter span{display:inline-block;margin-left:8px;font-size:11.5px;opacity:.55;}

.pf-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:clamp(16px,2vw,26px);}

/* one card shape for every category: the image is the card */
.pf-item{position:relative;display:block;border:0;border-radius:calc(var(--radius) + 2px);overflow:hidden;background:var(--navy-deep);
  transform-origin:center top;
  transition:transform .5s var(--ease),opacity .5s var(--ease),box-shadow .5s var(--ease);}
.pf-item:hover{transform:translateY(-8px);box-shadow:0 44px 84px -44px rgba(12,27,42,.55);}
.pf-item:focus-visible{outline:2px solid var(--blue);outline-offset:3px;}
.pf-item.is-hidden{display:none;}
.pf-item.is-entering{opacity:0;transform:translateY(26px) scale(.94);}
.pf-item.is-leaving{opacity:0;transform:translateY(-10px) scale(.97);}

.pf-thumb{display:block;position:relative;aspect-ratio:4/3.8;overflow:hidden;}
.pf-thumb img{width:100%;height:100%;object-fit:cover;object-position:top center;
  transition:transform .5s var(--ease),filter .5s var(--ease);}
/* hover: darker black and white, so the title reads over it */
.pf-item:hover .pf-thumb img{transform:scale(1.06);filter:grayscale(1) brightness(.44) contrast(1.06);}
.pf-thumb::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(12,27,42,0) 42%,rgba(12,27,42,.6) 100%);
  opacity:0;transition:opacity .5s var(--ease);}
.pf-item:hover .pf-thumb::after{opacity:1;}

/* icon badge */
.pf-go{position:absolute;right:16px;top:16px;z-index:3;width:40px;height:40px;border-radius:50%;background:rgba(255,255,255,.94);
  display:grid;place-items:center;color:var(--navy);
  opacity:0;transform:translateY(-8px) scale(.86);transition:opacity .5s var(--ease),transform .5s var(--ease);}
.pf-go svg{width:17px;height:17px;fill:none;stroke:currentColor;stroke-width:2.2;stroke-linecap:round;stroke-linejoin:round;}
.pf-item:hover .pf-go{opacity:1;transform:none;}

/* video tiles use the same badge as every other card - top right, on hover */
.pf-item--videos .pf-tri{width:17px;height:17px;margin-left:1px;}

/* title: hidden until hover */
.pf-meta{position:absolute;left:0;right:0;bottom:0;z-index:2;display:block;padding:20px 22px 22px;
  opacity:0;transform:translateY(14px);transition:opacity .5s var(--ease),transform .5s var(--ease);}
.pf-item:hover .pf-meta{opacity:1;transform:none;}
.pf-meta b{display:block;font-family:var(--font-display);font-size:16.5px;font-weight:700;line-height:1.3;color:#fff;
  text-shadow:0 2px 12px rgba(12,27,42,.5);}

/* ---- video lightbox ---- */
.vlx{position:fixed;inset:0;z-index:1200;display:grid;place-items:center;
  opacity:0;visibility:hidden;transition:opacity .42s var(--ease),visibility .42s;}
.vlx.is-open{opacity:1;visibility:visible;}
.vlx__scrim{position:absolute;inset:0;background:rgba(6,14,24,.9);backdrop-filter:blur(12px);
  opacity:0;transition:opacity .5s var(--ease);}
.vlx.is-open .vlx__scrim{opacity:1;}
.vlx__stage{position:relative;z-index:1;width:88%;max-width:1800px;
  opacity:0;transform:translateY(34px) scale(.94);
  transition:opacity .5s .06s var(--ease),transform .62s .06s var(--ease);}
.vlx.is-open .vlx__stage{opacity:1;transform:none;}
/* outro: keep .is-open so the element stays visible, and let .is-closing drive it out.
   Dropping .is-open in the same frame races the base rule and the exit snaps instead of eases. */
.vlx.is-open.is-closing .vlx__stage{opacity:0;transform:translateY(18px) scale(.965);
  transition:opacity .3s var(--ease) 0s,transform .34s var(--ease) 0s;}
.vlx.is-open.is-closing .vlx__scrim{opacity:0;transition:opacity .34s var(--ease) 0s;}
.vlx__frame{position:relative;aspect-ratio:16/9;width:100%;border-radius:var(--radius);overflow:hidden;background:#000;
  box-shadow:0 70px 140px -50px rgba(0,0,0,.92);}
.vlx__frame iframe{position:absolute;inset:0;width:100%;height:100%;border:0;}
.vlx__caption{margin-top:18px;font-family:var(--font-display);font-size:clamp(15px,1.5vw,19px);font-weight:600;color:#fff;text-align:center;}
.vlx__src{display:inline-flex;align-items:center;gap:7px;margin-left:14px;font-size:13px;font-weight:600;color:rgba(255,255,255,.66);
  border-bottom:1px solid rgba(255,255,255,.24);padding-bottom:1px;transition:color .3s,border-color .3s,gap .3s var(--ease);}
.vlx__src:hover{color:#fff;border-color:#fff;gap:11px;}
.vlx__src svg{width:13px;height:13px;fill:none;stroke:currentColor;stroke-width:2.2;stroke-linecap:round;stroke-linejoin:round;}
@media (max-width:640px){ .vlx__src{display:flex;justify-content:center;margin:10px 0 0;} }
.vlx__close{position:absolute;top:clamp(18px,3vh,34px);right:clamp(18px,3vw,40px);z-index:3;
  width:52px;height:52px;border:0;border-radius:50%;background:rgba(255,255,255,.12);color:#fff;cursor:pointer;
  display:grid;place-items:center;opacity:0;transform:scale(.8) rotate(-90deg);
  transition:opacity .4s .18s var(--ease),transform .5s .18s var(--ease),background .3s;}
.vlx.is-open .vlx__close{opacity:1;transform:none;}
.vlx.is-open.is-closing .vlx__close{opacity:0;transform:scale(.85) rotate(-90deg);
  transition:opacity .18s var(--ease) 0s,transform .22s var(--ease) 0s;}
.vlx__close:hover{background:var(--blue);}
.vlx__close svg{width:20px;height:20px;fill:none;stroke:currentColor;stroke-width:2.3;stroke-linecap:round;}

.pf-empty{padding:44px 0;color:var(--grey);font-size:15.5px;}

@media (max-width:1080px){
}
@media (max-width:900px){
}
@media (max-width:640px){
  .pf-grid{grid-template-columns:1fr;}
}
@media (prefers-reduced-motion:reduce){
    .bw,.pf-item,.cs__shot img,.pf-thumb img{transition-duration:.01ms;}
}

/* no-JS: the carousel degrades to a stack */
.no-js .csx__track{display:block;transform:none!important;}
.no-js .csx__slide{opacity:1!important;margin-bottom:clamp(40px,6vw,80px);}
.no-js .csx__slide .cs__body > *,.no-js .csx__slide .cs__media{opacity:1!important;transform:none!important;}
.no-js .csx-bar{display:none!important;}
