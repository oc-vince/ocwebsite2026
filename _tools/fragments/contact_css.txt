
/* ============================================================
   CONTACT
   ============================================================ */
.hero--book.hero--contact h1{white-space:normal;max-width:12ch;}
.hero--contact .hero__sub{max-width:74ch;}
.btn--blue{background:var(--blue);color:#fff;}
.btn--blue::before{background:var(--navy);}
.btn--blue:hover::before{transform:scaleX(1);}

/* ---- layout ---- */
.ct-wrap{display:grid;grid-template-columns:minmax(0,1.08fr) minmax(0,.92fr);gap:clamp(30px,4.6vw,72px);align-items:start;}

/* ---- form ---- */
.ct-form{background:#fff;border:1px solid var(--line);border-radius:calc(var(--radius) + 6px);padding:clamp(26px,3.4vw,46px);box-shadow:0 44px 90px -56px rgba(12,27,42,.42);}
.ct-form h2{font-size:clamp(24px,2.7vw,34px);font-weight:700;line-height:1.1;}
.ct-form > p{margin-top:12px;color:var(--grey);font-size:15.4px;line-height:1.7;max-width:52ch;}
.ct-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:clamp(14px,1.8vw,20px);margin-top:clamp(24px,3vw,34px);}
.ct-field{display:flex;flex-direction:column;gap:8px;}
.ct-field--wide{grid-column:1/-1;}
.ct-field label{font-size:12.5px;font-weight:600;letter-spacing:.1em;text-transform:uppercase;color:var(--navy);}
.ct-field label i{font-style:normal;color:var(--blue);}
.ct-field input,.ct-field textarea{width:100%;font-family:var(--font-body);font-size:15.5px;color:var(--ink);background:var(--paper);border:1.5px solid var(--line);border-radius:14px;padding:14px 16px;transition:border-color .28s,background .28s,box-shadow .28s;}
.ct-field textarea{min-height:150px;resize:vertical;line-height:1.65;}
.ct-field input::placeholder,.ct-field textarea::placeholder{color:var(--grey-light);}
.ct-field input:focus,.ct-field textarea:focus{outline:0;background:#fff;border-color:var(--blue);box-shadow:0 0 0 4px rgba(20,135,216,.14);}
.ct-field .ct-err{display:none;font-size:13px;color:#c8372d;}
.ct-field.is-bad input,.ct-field.is-bad textarea{border-color:#e0574b;background:#fdf4f3;}
.ct-field.is-bad .ct-err{display:block;}
.ct-foot{display:flex;flex-wrap:wrap;align-items:center;gap:16px;margin-top:clamp(24px,3vw,32px);}
.ct-foot small{flex:1;min-width:220px;font-size:13px;color:var(--grey-light);line-height:1.6;}
.ct-note{margin-top:20px;padding:14px 18px;border-radius:12px;background:var(--blue-soft);font-size:13.5px;color:var(--navy);line-height:1.6;}
.ct-ok{display:none;align-items:flex-start;gap:14px;margin-top:24px;padding:18px 20px;border-radius:14px;background:#eaf7ef;border:1px solid #bfe3cd;color:#1d6b3d;font-size:15px;line-height:1.65;}
.ct-ok.is-on{display:flex;}
.ct-ok svg{flex:0 0 auto;width:22px;height:22px;fill:none;stroke:#1d6b3d;stroke-width:2.2;stroke-linecap:round;stroke-linejoin:round;margin-top:1px;}
.ct-hp{position:absolute;left:-9999px;width:1px;height:1px;overflow:hidden;}

/* ---- details ---- */
.ct-card{background:linear-gradient(155deg,var(--navy) 0%,var(--navy-deep) 100%);color:#fff;border-radius:calc(var(--radius) + 6px);padding:clamp(26px,3.2vw,40px);position:relative;overflow:hidden;
  box-shadow:0 50px 100px -56px rgba(12,27,42,.8);}
.ct-card::before{content:"";position:absolute;right:-80px;top:-80px;width:300px;height:300px;border-radius:50%;background:radial-gradient(circle,rgba(20,135,216,.38),transparent 66%);pointer-events:none;}
.ct-card h2{position:relative;color:#fff;font-size:clamp(21px,2.3vw,28px);font-weight:700;}
.ct-quick{position:relative;display:grid;gap:2px;margin-top:22px;}
.ct-quick a,.ct-quick span.ct-line{display:grid;grid-template-columns:40px 1fr;gap:14px;align-items:center;padding:15px 0;border-bottom:1px solid rgba(255,255,255,.14);transition:padding-left .3s var(--ease);}
.ct-quick a:hover{padding-left:8px;}
.ct-quick i{width:40px;height:40px;border-radius:12px;background:rgba(124,195,239,.16);display:grid;place-items:center;}
.ct-quick i svg{width:19px;height:19px;fill:none;stroke:#7cc3ef;stroke-width:1.9;stroke-linecap:round;stroke-linejoin:round;}
.ct-quick b{display:block;font-family:var(--font-display);font-size:16px;font-weight:600;color:#fff;}
.ct-quick em{display:block;font-style:normal;margin-top:3px;font-size:12.5px;color:rgba(255,255,255,.6);}
.ct-offices{position:relative;display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:clamp(18px,2.2vw,28px);margin-top:clamp(24px,3vw,32px);padding-top:clamp(22px,2.6vw,28px);border-top:1px solid rgba(255,255,255,.14);}
.ct-office b{display:flex;align-items:center;gap:9px;font-family:var(--font-display);font-size:12px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:#7cc3ef;}
.ct-office address{margin-top:11px;font-style:normal;font-size:14.6px;line-height:1.72;color:rgba(255,255,255,.84);}
.ct-office a{display:inline-flex;align-items:center;gap:7px;margin-top:12px;font-size:13.5px;font-weight:600;color:#fff;transition:gap .3s var(--ease);}
.ct-office a:hover{gap:11px;color:#7cc3ef;}
.ct-legal{position:relative;margin-top:clamp(22px,2.6vw,28px);padding-top:20px;border-top:1px solid rgba(255,255,255,.14);font-size:13.4px;color:rgba(255,255,255,.6);line-height:1.7;}

/* ---- map ---- */
.map-head{display:flex;flex-wrap:wrap;align-items:flex-end;justify-content:space-between;gap:20px;margin-bottom:clamp(20px,2.4vw,28px);}
.map-toggle{display:flex;flex-wrap:wrap;gap:10px;}
.map-btn{position:relative;text-align:left;font-family:var(--font-display);background:#fff;border:1px solid var(--line);border-radius:16px;padding:14px 22px;cursor:pointer;transition:all .32s var(--ease);}
.map-btn:hover{border-color:var(--blue);transform:translateY(-3px);}
.map-btn.is-active{background:var(--navy);border-color:var(--navy);}
.map-btn b{display:block;font-size:14.5px;font-weight:700;color:var(--ink);transition:color .3s;}
.map-btn span{display:block;margin-top:4px;font-family:var(--font-body);font-size:12.5px;color:var(--grey);transition:color .3s;}
.map-btn.is-active b{color:#fff;}
.map-btn.is-active span{color:rgba(255,255,255,.66);}
.map-frame{position:relative;aspect-ratio:21/9;min-height:340px;border-radius:calc(var(--radius) + 6px);overflow:hidden;background:var(--mist);border:1px solid var(--line);box-shadow:0 44px 90px -56px rgba(12,27,42,.45);}
.map-frame iframe{position:absolute;inset:0;width:100%;height:100%;border:0;filter:grayscale(.24) contrast(1.03);transition:opacity .45s var(--ease);}
.map-frame.is-swapping iframe{opacity:0;}
.map-pin{position:absolute;left:clamp(16px,2vw,26px);bottom:clamp(16px,2vw,26px);z-index:2;display:flex;align-items:center;gap:13px;background:rgba(255,255,255,.96);backdrop-filter:blur(8px);border-radius:16px;padding:14px 20px 14px 16px;box-shadow:0 20px 44px -20px rgba(12,27,42,.5);max-width:calc(100% - 32px);}
.map-pin i{flex:0 0 auto;width:38px;height:38px;border-radius:11px;background:var(--blue);display:grid;place-items:center;}
.map-pin i svg{width:19px;height:19px;fill:none;stroke:#fff;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;}
.map-pin b{display:block;font-family:var(--font-display);font-size:14.5px;font-weight:700;color:var(--ink);}
.map-pin span{display:block;margin-top:3px;font-size:12.8px;color:var(--grey);}

@media (max-width:980px){
  .ct-wrap{grid-template-columns:1fr;}
  .ct-offices{grid-template-columns:1fr;}
}
@media (max-width:600px){
  .ct-grid{grid-template-columns:1fr;}
  .map-frame{aspect-ratio:4/3;}
}
@media (prefers-reduced-motion:reduce){
  .map-btn,.map-frame iframe,.ct-quick a{transition-duration:.01ms;}
}
